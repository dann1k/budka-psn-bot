{
  "name": "budka-psn-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Telegram PSN bot deployed as a Supabase Edge Function.",
  "type": "module",
  "scripts": {
    "check": "npx -y deno check supabase/functions/telegram-webhook/index.ts",
    "migrate:dry-run": "node scripts/migrate-sqlite-to-supabase.mjs",
    "migrate:apply": "node scripts/migrate-sqlite-to-supabase.mjs --apply"
  },
  "engines": {
    "node": ">=22"
  }
}
